version: "3.9"

services:
  logmonitor:
    build: .
    container_name: logmonitor
    volumes:
      - /home/acintyo/b2b/logs:/home/acintyo/b2b/logs
    expose:
      - "6000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
